{
  "name": "@circlesac/vlt-cli",
  "description": "1Password-compatible secrets CLI for Circles Vault",
  "type": "module",
  "bin": {
    "cvlt": "bin/cvlt.mjs"
  },
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "bun build --compile src/index.ts --outfile cvlt",
    "dev": "bun run src/index.ts",
    "test": "bun test src/",
    "postinstall": "node bin/install.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/circlesac/cvlt.git"
  },
  "dependencies": {
    "citty": "^0.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
